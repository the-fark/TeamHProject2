//Matthew Sanders
//This class will return the value of the nickel if the coin lands on heads when flipped, and return a value of 0 if the coin lands on tails.
//Last updated 3/10/20
package project2;
public class nickel {
	//Creates an object that tells whether the nickel has landed on heads or tails
	flipC flip = new flipC();
	
	public double nickelM() {
		double value;
		//Returns a value of 0.05 if the nickel lands on heads, or returns a value of 0 if the nickel lands on tails
		if (flip.flipM()) {
			value = 0.05;
		}else {
			value = 0;
		}
		return value;
	}

}
